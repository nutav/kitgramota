<% content_for :title, 'Цены А' %>
<h1>Цены</h1>

<%= link_to new_price_path, class: 'button info' do %>
    <i class="fi-plus"></i> Создать новую ценовую категорию
<% end %>

<% if @prices.any? %>
<table style="width: 100%;">
  <thead>
    <tr>
      <th>#</th>
      <th>Курс</th>
      <th>Тип</th>
      <th>Стоимость 1 ак. ч.</th>
      <th>В субботу?</th>
      <th>Индивидуальное?</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @prices.each do |price| %>
      <tr>
        <td><%= price.id %></td>
        <td><%= price.level.name %></td>
        <td><%= price.type.name %></td>
        <td><%= price.sum %> руб.</td>
        <td><%= price.saturday? ? 'да' : '' %></td>
        <td><%= price.individual? ? 'да' : '' %></td>
        <td>
          <%= link_to edit_price_path(price), class: 'button tiny', title: 'Редактировать' do %>
              <i class="fi-pencil"></i>
          <% end %>
          <%= link_to price_path(price), method: :delete, class: 'button alert tiny', title: 'Удалить', data: {confirm: 'Вы уверены?'} do %>
              <i class="fi-x"></i>
          <% end %>
        </td>
      </tr> 
    <% end %>
  </tbody>
</table>
<% else %>
  <p>Пока не было добавлено ни одной ценовой категории</p>
<% end %>