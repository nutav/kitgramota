<% content_for :title, @topic.name %>
<h1>
	<div class="row">
	<div class="small-7 columns">
		<%= @topic.name %>
	</div>
	<div class="small-3 columns">	
		<div class="yashare-auto-init" data-yashareL10n="ru"
	     data-yashareQuickServices="vkontakte,facebook,twitter" data-yashareTheme="counter"
	     data-yashareLink="<%= topic_url(@topic)%>" data-yashareTitle="<%= @topic.name %>" data-yashareDescription="<%= raw @topic.description.truncate(90) %>">
	 	</div>
	</div> 	
 </div>
</h1>

<div><%= raw @topic.description %></div>

